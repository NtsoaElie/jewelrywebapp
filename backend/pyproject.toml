[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Ntsoa Elie", email = "ntsoaelie@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "python-dotenv>=1.2.3",
    "supabase>=2.31.0",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
